# 自転車製造会社経営シミュレータ

---

## 1. コンセプト

プレイヤーは自転車製造会社の経営者部品を市場から買い付け，設備で自転車を製造し，市場へ売る
NPC企業(供給・製造・販売)も同じルールで自律的に売買・設備改良を行い，需給によって価格が変動する
**倒産せずに目標資本金へ到達する**ことが目的

## 2. スコープ方針

| 方針             | 内容                                                            |
| ---------------- | --------------------------------------------------------------- |
| 進行             | ターン制1ターン = 1営業日プレイヤー入力 → ターン解決            |
| サプライチェーン | 部品 → 自転車 の1段のみ自転車1台 = 部品1個                      |
| 市場             | 部品市場・自転車市場の2つコールオークション方式                 |
| 最終需要         | 販売会社需要 = 消費者需要販売会社の在庫が需要関数に従い消滅する |
| グラフィック     | 2D，egui中心で演出は最後                                        |

## 3. ターン進行

各ターンは以下のフェーズを順に解決する

```
[入力] プレイヤーが価格・発注方針・改良プロジェクトを設定 → 「ターン終了」
[解決]
  1. 改良進行     : 全社の進行中プロジェクトに成果Δを加算，費用を徴収
  2. 生産         : 供給会社が部品，製造会社が自転車を生産(不良品は廃棄)
  3. 部品市場約定 : 供給(売) × 製造(買) を板寄せ
  4. 自転車市場約定: 製造(売) × 販売(買) を板寄せ
  5. 最終販売     : 販売会社ごとに需要関数で自転車が消滅，売上計上
  6. 維持費徴収   : 固定維持費 + 稼働維持費
  7. 倒産判定     : 資金 < 0 の企業を除外(プレイヤーならゲームオーバー)
  8. NPC意思決定  : 次ターンの価格・発注・改良を決定
```

## 4. 会社種別

| 種別           | 買う             | 作る/処理 | 売る       | 備考                                      |
| -------------- | ---------------- | --------- | ---------- | ----------------------------------------- |
| 部品供給会社   | なし(無から生産) | 部品      | 部品市場   | 原材料段省略のため生産コスト = 稼働維持費 |
| 自転車製造会社 | 部品市場         | 自転車    | 自転車市場 | プレイヤーはこれNPCも複数                 |
| 自転車販売会社 | 自転車市場       | (販売)    | 需要関数へ | 消費者の代理全員NPC                       |

初期構成(案): 供給×3，製造×3(うち1がプレイヤー)，販売×3

## 5. 需要モデル

- 販売会社 i は販売価格 `p_i` を設定し，毎ターン次の量が在庫から消滅して売上になる:

  `q_i = min(在庫_i, (D(turn)/N_ret) * (P_ref / p_i)^e)`

  - `D(turn)`: 基礎需要緩やかなトレンド + 周期(週末等) + ノイズ**全エージェントから隠蔽**され，直接参照できない
  - `N_ret`: 生存中の販売会社数`e`: 価格弾力性(既定: 1.5)
- 需要関数は予測不能である前提のため，販売会社は**結果からのルールベース適応**のみ行う(§8)
- 安全弁: `D(turn)` に下限を設け，全販売会社が同時倒産して製造会社の売り先が消える連鎖崩壊を防ぐ

## 6. 設備パラメータと改良(無段階)

各企業は設備を1基持ち，5つの連続値パラメータを持つ

| パラメータ | 効果(改善方向)                    |
| ---------- | --------------------------------- |
| 製造速度   | ターンあたり生産数 ↑              |
| 固定維持費 | 毎ターン固定支出 ↓                |
| 稼働維持費 | 生産1個あたり費用 ↓               |
| 不良品率   | 廃棄率 ↓(下限 1%)                 |
| 資源効率   | 自転車1台の必要部品数 ↓(下限 2.0) |

### 改良プロジェクト

プレイヤー(およびNPC)は次の3つを設定するだけ:

1. **選択要素** `k`: 5パラメータのうちどれを改善するか
2. **費用/ターン** `c`: 毎ターン支払う投資額
3. **ターン数** `T`: プロジェクト期間

進行中は毎ターン，選択要素に成果を加算する:

```
Δ = ε * f(v_k, k, c) * g(t, T)
```

- `ε`: 乱数(例: 一様 `U[0.5, 1.5]`)**成果は確率的**で，同じ投資でも結果がぶれる
- `f(v_k, k, c)`: 費用効果例: `f = base_k * c^0.7 / (1 + improved(v_k))`
  - `c^0.7`: 費用の限界効果逓減(札束で殴るほど効率が落ちる)
  - `improved(v_k)`: 初期値からの累積改善度改善が進むほど次の1歩が重くなる
- `g(t, T)`: 期間内進捗カーブ例: 山型 `g = 4t(T-t)/T²`
  - 序盤は立ち上がりが鈍く，中盤に伸び，終盤に収束 → 「短期集中 × 高費用」と「長期 × 低費用」で性格が変わる
- 同時プロジェクトは**1本まで**(P0)中断可(支払済費用は返らない)並行はP2
- UIには `E[Δ] = f * g`(ε の期待値1)を**見込み値**として表示し，実績との乖離を体感させる

## 7. プレイヤー操作(製造会社)

- 価格設定: 部品の**最大買付価格** / 自転車の**最小販売価格**(スライダ)
- 発注方針: 目標在庫ターン数(買付・売出数量を自動算出)
- 改良: 選択要素・費用/ターン・ターン数を設定して開始/中断
- ターン終了ボタン(まとめてNターン進める送りもP1で用意)

## 8. NPC AI(ルールベース)

需要関数・乱数の内部は参照させず，**観測結果のみ**から適応する

- **販売会社**(需要が読めない前提):
  - 売り切れ(在庫0で需要を取り逃した疑い) → 販売価格 +δ%，次ターン買付量 ↑
  - 売れ残り率が閾値超過 → 販売価格 −δ%，買付量 ↓
  - 買付指値は「現販売価格 − 目標マージン」を上限とする
- **供給/製造会社**: 直近約定価格を基準に，在庫乖離率で指値を上下(`price = last * (1 ± k·乖離率)`)
- **改良判断**: 資金が閾値超過時，各パラメータの `E[Δ]` から粗ROIを見積り最大の要素へ投資期間・費用は性格係数で決定
- **個体差**: 強気度・投資性向・目標マージンを企業ごとにランダム付与

## 9. ECS 設計(概略)

```rust
/// 企業の種別システムの振る舞い分岐に用いる
#[derive(Component)]
enum CompanyKind { Supplier, Manufacturer, Retailer }

/// 資金負になったターン終端で倒産処理
#[derive(Component)]
struct Wallet(i64);

/// 品目別在庫(部品・自転車)
#[derive(Component)]
struct Inventory { parts: u32, bikes: u32 }

/// 設備の連続値パラメータ(改良で無段階に変化)
#[derive(Component)]
struct Facility { speed: f32, fixed_cost: f32, run_cost: f32, defect: f32, efficiency: f32 }

/// 進行中の改良プロジェクト(1社1本)
#[derive(Component)]
struct Upgrade { target: FacilityParam, cost_per_turn: i64, total_turns: u32, elapsed: u32 }

/// 売買方針プレイヤーはUI，NPCはAIシステムが書き込む
#[derive(Component)]
struct TradePolicy { max_buy: i64, min_sell: i64, target_stock_turns: u8 }

/// プレイヤー企業マーカー / NPC性格係数
#[derive(Component)] struct Player;
#[derive(Component)] struct AiTraits { aggressiveness: f32, invest_bias: f32, margin: f32 }

/// 市場(部品/自転車)板と直近価格を保持
#[derive(Component)]
struct Market { good: Good, last_price: i64, volume: u32, book: OrderBook }

/// 隠蔽された基礎需要 D(turn)エージェントからは参照禁止
#[derive(Resource)]
struct DemandCurve { base: f32, trend: f32, noise_seed: u64 }
```

- ターン解決は `States`(入力待ち ⇄ 解決中)+ §3の順のシステムチェーンで実装
- 板寄せと `Δ` 計算は純粋関数に分離し，単体テストを書く(`fn clear(book) -> Vec<Trade>`, `fn upgrade_delta(...) -> f32`)

## 10. UI

- 左: 自社パネル(資金，在庫，設備値，改良プロジェクト設定と進捗/見込みΔ)
- 中央: 2市場の価格チャートと出来高，自社の販売実績
- 右: 企業一覧(業種・資金・在庫の概況，倒産表示)
- 上: ターン数，ターン終了ボタン，目標資本金への進捗バー
- 推奨クレート: `bevy_egui`(チャートは egui `Plot`)

## 11. 勝敗条件

- **勝利**: 資本金が目標額(例: 初期資金の10倍)に到達到達ターン数を記録
- **敗北**: 倒産

## 13. 優先度とカットライン

- **P0(必須)**: 3業種，2市場，板寄せ，需要関数，価格/発注設定，改良プロジェクト，勝敗
- **P1(できれば)**: 価格チャート，Nターン送り，NPC個体差，セーブ/ロード
- **P2(余裕があれば)**: イベント，改良の見込み分布表示


## 14. リスクと対策

- **経済の連鎖崩壊**(販売会社全滅 → 製造の売り先消失): `D(turn)` の下限と，倒産企業スロットへの新規参入(資金リセットで再スポーン)を用意
- **確率的改良の不透明感**: 見込み `E[Δ]` と実績Δの両方をUIに出すε の分散は係数1つで調整可能にする
- **f/g/D のバランス地獄**: 全係数を1つの定数モジュールに集約し，Day2午後に集中調整ヘッドレスで100ターン自動実行するバランステストを1本書く
- **板寄せ/Δ計算のバグ**: 純粋関数化 + 単体テスト
